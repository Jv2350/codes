// Q1 Create a const object called "product" to store information shown in the picture.

const product = {
    name:"fan",
    price: 5000,
    isOff: false,
};

console.log(product)

// Q2. Create a const object called "profile" to store information shown in the picture.

const profile = {
    userName: "_btw_i_m_jayesh",
    followers: 123,
    following: 234,
    isActive: true
};

console.log(profile)